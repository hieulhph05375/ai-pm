# ==============================================================================
# Project Management System - Production Environment Configuration
# ==============================================================================

# 1. Database Configuration (Shared with Compose)
# ------------------------------------------------------------------------------
DB_USER=devops
DB_PASSWORD=rbNyb3cTNLxeM
DB_NAME=pm_db
DB_HOST=localhost
DB_PORT=5433

# 2. Backend Configuration
# ------------------------------------------------------------------------------
PORT=8081
# Full connection string for the backend (use 'pm_db' as host inside Docker)
DATABASE_URL=postgres://devops:rbNyb3cTNLxeM@pm_db:5432/pm_db?sslmode=disable
JWT_SECRET=433c0c84-0ce7-4891-af6d-9aaec0d319ce
ENV=production

# 4. Frontend Configuration
# ------------------------------------------------------------------------------
# (Proxied via Nginx in Docker - no separate env needed for browser)
VITE_API_BASE_URL=/api
